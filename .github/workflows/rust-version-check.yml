# @file rust-version-check.yml
#
# A workflow that checks for changes to the channel field in rust-toolchain.toml on pull requests.
#
##
# Copyright (c) Microsoft Corporation.
# SPDX-License-Identifier: Apache-2.0
##

name: Rust Version Change Detection

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize]
    paths:
      - 'rust-toolchain.toml'

jobs:
  check-rust-version:
      permissions:
        pull-requests: write
        contents: read

      uses: makubacki/patina-devops/.github/workflows/RustVersionCheck.yml@switch_some_workflows_to_gh_app_test
      secrets: inherit
